
Процедура ПередЗаписью(Отказ)
	Если не ОбменДанными.Загрузка тогда
		Если СтрНайти(ПроизвольныйАлгоритм,".Записать(") <> 0 
			или СтрНайти(ПроизвольныйАлгоритм,".Удалить(") <> 0 тогда
			Отказ = Истина;
			Сообщение = Новый СообщениеПользователю;
			Сообщение.Текст = "Нельзя выполнять операции с данными в правилах регистрации объектов на обмен!!!";
			Сообщение.Сообщить();
		КонецЕсли;
	КонецЕсли;
КонецПроцедуры

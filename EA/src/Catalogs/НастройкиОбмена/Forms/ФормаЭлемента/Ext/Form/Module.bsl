
&НаКлиенте
Процедура ПередЗаписью(Отказ, ПараметрыЗаписи)
	Если СтрНайти(Объект.ПроизвольныйАлгоритм,".Записать(") <> 0 
		или СтрНайти(Объект.ПроизвольныйАлгоритм,".Удалить(") <> 0 тогда
		Отказ = Истина;
		Сообщение = Новый СообщениеПользователю;
		Сообщение.Текст = "Нельзя выполнять операции с данными в правилах регистрации объектов на обмен!!!";
		Сообщение.Сообщить();
	КонецЕсли;
КонецПроцедуры

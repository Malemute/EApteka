
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Склад = Параметры.Поставщик;
	Если не ЗначениеЗаполнено(Склад) тогда
		Склад = ?(ЗначениеЗаполнено(ПараметрыСеанса.ОсновнойСклад),ПараметрыСеанса.ОсновнойСклад,Справочники.МестаХранения.ПустаяСсылка());
	КонецЕсли;
	Список.Параметры.УстановитьЗначениеПараметра("ТекущаяДата",ТекущаяДата());
	Список.Параметры.УстановитьЗначениеПараметра("Отдел",Склад);
	
КонецПроцедуры
